#include "RenderView.h"

namespace Destiny
{
	RenderView::RenderView(long long nativeWindow, unsigned int windowWidth, unsigned int windowHeight) :
		m_nativeWindow(nativeWindow),
		m_windowWidth(windowWidth),
		m_windowHeight(windowHeight)
	{

	}
}